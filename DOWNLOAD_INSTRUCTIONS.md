# 📥 Инструкция по скачиванию CRM-системы на компьютер

## Способ 1: Скачать ZIP-архив (Рекомендуется)

### Через команду в терминале:
```bash
# Создать ZIP-архив проекта
cd /home/user/crm-system
zip -r crm-system.zip . -x "node_modules/*" ".git/*"
```

Затем скачайте файл `crm-system.zip` через файловый менеджер.

### После скачивания на компьютер:
```bash
# Распаковать архив
unzip crm-system.zip -d crm-system

# Перейти в директорию
cd crm-system

# Установить зависимости
npm install

# Запустить проект
npm run dev
```

---

## Способ 2: Через Git клонирование (если есть GitHub)

### На сервере:
```bash
# Перейти в директорию проекта
cd /home/user/crm-system

# Настроить Git (если еще не настроен)
git config user.name "Your Name"
git config user.email "your@email.com"

# Добавить все файлы
git add .
git commit -m "CRM System complete"

# Создать репозиторий на GitHub и загрузить
git remote add origin https://github.com/your-username/crm-system.git
git push -u origin main
```

### На компьютере:
```bash
# Клонировать репозиторий
git clone https://github.com/your-username/crm-system.git

# Перейти в директорию
cd crm-system

# Установить зависимости
npm install

# Запустить проект
npm run dev
```

---

## Способ 3: Скачать через ProjectBackup

Если доступен инструмент ProjectBackup:
```bash
# Создать резервную копию
# ProjectBackup автоматически создаст tar.gz файл
```

Затем скачайте созданный `.tar.gz` файл.

### На компьютере:
```bash
# Распаковать архив
tar -xzf crm-system-backup.tar.gz

# Перейти в директорию
cd home/user/crm-system

# Установить зависимости
npm install

# Запустить проект
npm run dev
```

---

## Способ 4: Копирование файлов вручную

Если другие способы не работают:

1. **Создать структуру на компьютере:**
```bash
mkdir crm-system
cd crm-system
npm init -y
```

2. **Скопировать файлы:**
   - Скопируйте содержимое `package.json`
   - Установите зависимости: `npm install`
   - Скопируйте все файлы из директорий:
     - `src/`
     - `public/`
     - Конфигурационные файлы (`vite.config.js`, `index.html` и т.д.)

3. **Запустить проект:**
```bash
npm run dev
```

---

## Проверка установки

После установки проект должен запуститься на `http://localhost:5173`

### Команды для разработки:
```bash
npm run dev          # Запуск dev-сервера
npm run build        # Сборка для production
npm run preview      # Просмотр production-сборки
```

### Доступ к системе:
- **URL**: http://localhost:5173
- **Логин**: admin
- **Пароль**: password

---

## Требования к системе

- Node.js v16 или выше
- npm v7 или выше
- Современный браузер (Chrome, Firefox, Safari, Edge)

---

## Возможные проблемы

### Ошибка "node_modules not found"
```bash
npm install
```

### Ошибка "Cannot find module"
```bash
rm -rf node_modules package-lock.json
npm install
```

### Порт 5173 занят
Измените порт в `vite.config.js`:
```js
export default defineConfig({
  server: {
    port: 3000 // или другой свободный порт
  }
})
```

---

## Дополнительная информация

После успешного запуска:
1. Откройте браузер на `http://localhost:5173`
2. Войдите с учетными данными:
   - **Админ**: admin / password
   - **ТП**: support1 / password
   - **Инженер**: engineer1 / password
   - **Монтажник**: installer1 / password
   - **Руководитель**: manager / password

3. База данных SQLite сохраняется в localStorage браузера
4. Для экспорта/импорта БД используйте кнопки в настройках

---

## Контакты и поддержка

При возникновении проблем проверьте:
- Установлены ли все зависимости
- Правильность версии Node.js
- Логи в консоли браузера (F12)
- Логи в терминале
